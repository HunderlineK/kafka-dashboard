// https://kafka.js.org/docs/configuration
const cluseters = [{
    meta: {
      id: string
    },
    config: {
      clientId: string,
      brokers: string[],
      authenticationTimeout: number,
      ssl: boolean,
      sasl: {
        mechanism: 'plain', // scram-sha-256 or scram-sha-512
        username: string',
        password: string
      },
    }
}]

module.exports = cluseters
